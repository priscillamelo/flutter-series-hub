import 'package:flutter/material.dart';

class InfoDataPage extends StatelessWidget {
  const InfoDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
